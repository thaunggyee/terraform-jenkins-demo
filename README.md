Prerequities

```bash
make a jenkins server and install terraform on jenkins server
install terraform plugin in jenkins
add terraform Name- Terraform and Install directory- /usr/bin global toolnconfigurations
